-- ============================================================
-- 사용자정보 내선번호 제거
-- - EXT_NO는 구 프로젝트 잔재 컬럼이므로 사용자 관리 및 사용자 선택 기능에서 제외한다.
-- - MariaDB 10.6 기준 IF EXISTS 사용 가능.
-- ============================================================
ALTER TABLE user_info
    DROP COLUMN IF EXISTS EXT_NO;
